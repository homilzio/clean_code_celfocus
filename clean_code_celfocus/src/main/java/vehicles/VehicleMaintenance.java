package vehicles;

/**
 * TODO implement the body of the class
 * Schedule maintenance
 * Send maintenance reminder
  */
public abstract class VehicleMaintenance implements Vehicle{

    // ZOMBIE CODE - SHOULD be deleted
    /*
    public static void checkCheckHighCiclomatic() {
        for (; ; ) {
            boolean isHighCiclomatic = false;
            if (!isHighCiclomatic) {
                for (; ; ) {
                    if (isValidValue(isHighCiclomatic)) {
                        for (; ; ) {
                            if (isValidValue(isHighCiclomatic)) {
                                return;
                            }
                            for (; ; ) {
                                if (isValidValue(isHighCiclomatic)) {
                                    return;
                                }
                            }
                        }
                        }
                    }
                }

            }
        }

    }
     */

    // TODO implement this
    private static boolean isValidValue(boolean isHighCiclomatic) {
        return false;
    }


}
